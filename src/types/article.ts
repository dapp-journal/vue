export interface IKeyword {
  id: number;
  name: string;
  user: string;
  timestamp: number;
}

export interface IAuthor {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  doi: string;
  user: string;
  timestamp: number;
}
